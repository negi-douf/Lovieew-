<div class="container">
  <div class="col col-md-8 col-md-offset-2 col-sm-offset-10 col-sm-offset-1">
    <div class="title">
      <h1>ランキング</h1>
    </div>

    <% @reviews.each do |review| %>
      <div class="review">
        <%= render "review", review: review %>
      </div>
      <br />
    <% end %>
    <%= paginate @reviews %>
  </div>
</div>
